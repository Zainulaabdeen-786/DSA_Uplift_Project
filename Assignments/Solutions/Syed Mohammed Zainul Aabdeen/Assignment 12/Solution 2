#include <bits/stdc++.h>
using namespace std;

int main(){
    int n,col_no=0;
    cin>>n;
    while(n--){
        string a;
        cin>>a;
        for(int i=0;i<a.size();i++){
            col_no = col_no*26 + (a[i]-'A')+1;
        }
    }
    cout<<col_no<<endl;
    return 0;
}
